description: Bad Request - Failed to create contact
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: 'Failed to create contact.'
        data:
          type: object
